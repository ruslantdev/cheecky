import {lazy} from 'react';

export const LayoutDefault = lazy(() => import('@modules/LayoutDefault'));

export const HomePage = lazy(() => import('@modules/pages/Home'));

export const AboutPage = lazy(() => import('@modules/Subscriptions'));
